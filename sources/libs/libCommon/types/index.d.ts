declare namespace libCommon {
  namespace constants {
    export enum Degrees {
      MIN_DEGREE = 0,
      MAX_DEGREE = 359,
    }

    export enum Directions {
      STRAIGHT = 'straight',
      LEFT = 'left',
      RIGHT = 'right',
    }
  }
}